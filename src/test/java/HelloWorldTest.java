import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class HelloWorldTest {

    @Test
    void main() {
        assertEquals(0, 0);
        // assertTrue("Mike le bg".length() > "Nathan".length());
    }
}